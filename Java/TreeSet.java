
public class TreeSet {

	// TreeSet is one of the most important implementations of the SortedSet interface in Java that uses a Tree for storage
	//The implementation in a TreeSet is not synchronized in a sense that if multiple threads access a tree set concurrently,
	// and at least one of the threads modifies the set, it must be synchronized externally.
	// TreeSet ts = new TreeSet();
	// Set syncSet = Collections.synchronziedSet(ts); 
	
	// TreeSet t = new TreeSet();
	// TreeSet t = new TreeSet(Comparator comp);
	// TreeSet t = new TreeSet(Collection col);
	// TreeSet t = new TreeSet(SortedSet s);
	
	// first()
	// last()
	// subSet(Object low_element, Object up_element)   // The lower limit is included, the upper limit is excluded
}
